{
	# Replace this with the real domain at deploy time.
	email admin@example.com
}

your-domain.com {
	encode gzip zstd

	handle_path /media/* {
		root * /srv/media
		file_server
	}

	reverse_proxy web:8000
}
